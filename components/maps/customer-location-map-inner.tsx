"use client";
import {useRef} from "react";import {divIcon} from "leaflet";import {MapContainer,Marker,Popup,TileLayer} from "react-leaflet";import "leaflet/dist/leaflet.css";
export function CustomerLocationMapInner({latitude,longitude,address,className=""}:{latitude:number;longitude:number;address:string;className?:string}){const container=useRef<HTMLDivElement>(null);const encoded=encodeURIComponent(`${latitude},${longitude}`);const marker=divIcon({className:"yesmoto-map-pin",html:"<span>Y</span>",iconSize:[36,44],iconAnchor:[18,44]});const directions=typeof navigator!=="undefined"&&/iPhone|iPad|Mac/i.test(navigator.userAgent)?`https://maps.apple.com/?daddr=${encoded}`:`https://www.google.com/maps/dir/?api=1&destination=${encoded}`;return <div className={`customer-location-map ${className}`} ref={container}><MapContainer center={[latitude,longitude]} zoom={15} scrollWheelZoom className="leaflet-map"><TileLayer attribution='&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a>' url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"/><Marker position={[latitude,longitude]} icon={marker}><Popup><a href={`https://www.openstreetmap.org/?mlat=${latitude}&mlon=${longitude}#map=17/${latitude}/${longitude}`} target="_blank" rel="noreferrer">{address}</a></Popup></Marker></MapContainer><div className="map-actions"><a href={directions} target="_blank" rel="noreferrer">Get directions</a><a href={`https://www.waze.com/ul?ll=${encoded}&navigate=yes`} target="_blank" rel="noreferrer">Waze</a><button type="button" onClick={()=>void container.current?.requestFullscreen()}>Fullscreen</button></div></div>}
