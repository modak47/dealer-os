import Link from "next/link";
import { getStripe } from "@/lib/stripe";
import { fulfilReservationCheckout } from "@/lib/stripe-reservations";
export const dynamic = "force-dynamic";
export default async function ReservationSuccess({ searchParams }: { searchParams: Promise<{ session_id?: string }> }) { const { session_id } = await searchParams; let paid = false; if (session_id?.startsWith("cs_")) { try { const session = await getStripe().checkout.sessions.retrieve(session_id); paid = session.payment_status === "paid"; if (paid) await fulfilReservationCheckout(session); } catch (error) { console.error("Unable to verify reservation return", error instanceof Error ? error.message : error); } } return <main className="reservation-result"><section><span>{paid ? "✓" : "!"}</span><p>YESMOTO ONLINE RESERVATION</p><h1>{paid ? "YOUR MOTORCYCLE IS RESERVED" : "PAYMENT IS BEING CONFIRMED"}</h1><p>{paid ? "Thank you. Your £99 reservation has been received and the motorcycle is now held for you. A member of the YesMoto team will contact you shortly." : "We have not yet received confirmation from Stripe. Please do not make another payment. Contact us if this message does not update."}</p><div><Link href="/stock">Browse motorcycles</Link><Link href="/contact">Contact YesMoto</Link></div></section></main>; }
