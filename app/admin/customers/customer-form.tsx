"use client";
import { FormEvent,useState } from "react";
import { useRouter } from "next/navigation";
export function CustomerForm(){const router=useRouter();const [saving,setSaving]=useState(false);const [error,setError]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setSaving(true);setError("");const form=new FormData(event.currentTarget);const response=await fetch("/api/crm/customers",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(form))});const result=await response.json() as {customer?:{id:string};error?:string};setSaving(false);if(!response.ok||!result.customer){setError(result.error||"Unable to add customer.");return}router.push(`/admin/customers/${result.customer.id}`);router.refresh()}return <form className="stock-editor-panel crm-form" onSubmit={submit}><div className="stock-form-grid"><label><span>First name</span><input name="first_name" required/></label><label><span>Last name</span><input name="last_name" required/></label><label><span>Email</span><input name="email" type="email"/></label><label><span>Phone</span><input name="phone" type="tel"/></label><label><span>Postcode</span><input name="postcode"/></label><label className="full"><span>Notes</span><textarea name="notes" rows={5}/></label></div>{error&&<p className="stock-save-message">{error}</p>}<div className="crm-form-actions"><button className="admin-primary" disabled={saving}>{saving?"Creating…":"Create customer"}</button></div></form>}
