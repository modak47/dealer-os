"use client";
import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import {AddressLookup,type AddressValue} from "@/components/common/AddressLookup";
const emptyAddress:AddressValue={buildingNumber:"",buildingName:"",addressLine1:"",addressLine2:"",addressLine3:"",town:"",county:"",postcode:"",country:"United Kingdom",latitude:null,longitude:null};
export function CustomerForm(){const router=useRouter();const [saving,setSaving]=useState(false),[error,setError]=useState(""),[address,setAddress]=useState<AddressValue>(emptyAddress);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setSaving(true);setError("");const form=new FormData(event.currentTarget);const response=await fetch("/api/crm/customers",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...Object.fromEntries(form),house_name_number:[address.buildingNumber,address.buildingName].filter(Boolean).join(" "),address_line_1:address.addressLine1,address_line_2:address.addressLine2,address_line_3:address.addressLine3,city:address.town,county:address.county,postcode:address.postcode,country:address.country,latitude:address.latitude,longitude:address.longitude})});const result=await response.json() as {customer?:{id:string};error?:string};setSaving(false);if(!response.ok||!result.customer){setError(result.error||"Unable to add customer.");return}router.push(`/admin/customers/${result.customer.id}`);router.refresh()}return <form className="stock-editor-panel crm-form" onSubmit={submit}><div className="stock-form-grid"><label><span>First name</span><input name="first_name" required/></label><label><span>Last name</span><input name="last_name" required/></label><label><span>Email</span><input name="email" type="email"/></label><label><span>Phone</span><input name="phone" type="tel"/></label><div className="full"><AddressLookup value={address} onChange={setAddress}/></div><label className="full"><span>Notes</span><textarea name="notes" rows={5}/></label></div>{error&&<p className="stock-save-message">{error}</p>}<div className="crm-form-actions"><button className="admin-primary" disabled={saving}>{saving?"Creating…":"Create customer"}</button></div></form>}
