import {dealership} from "@/config/dealership"; import {AdminPage} from "../dashboard/page"; import {Toggle} from "../stock/page";
const channels=[{name:`${dealership.dealerName} website`,initial:dealership.dealerName[0],state:"Connected",on:true,copy:"Publish stock instantly to your customer-facing website."},{name:"Auto Trader",initial:"AT",state:"Integration pending",on:false,copy:"API credentials and automated stock publishing will be added in a future release."},{name:"eBay Motors",initial:"e",state:"Integration pending",on:false,copy:"Create and manage eBay listings from DealerOS once connected."}];
export default function Channels(){return <AdminPage title="Sales channels" sub="Control where your vehicle stock is advertised."><div className="channel-grid">{channels.map(c=><article key={c.name}><div className="channel-head"><b>{c.initial}</b><Toggle on={c.on}/></div><h2>{c.name}</h2><span className={c.on?"connected":"pending"}>{c.state}</span><p>{c.copy}</p><button>{c.on?"Manage channel":"Configure integration"}</button></article>)}</div><div className="integration-note"><b>Integration roadmap</b><p>Marketplace connections are placeholders in V1. Publishing controls will connect to Auto Trader and eBay APIs when credentials and commercial access are available.</p></div></AdminPage>}
