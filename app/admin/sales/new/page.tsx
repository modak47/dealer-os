import {AdminPage} from "../../dashboard/page";import {getSupabaseAdmin} from "@/lib/supabase/admin";import {SalesWizard} from "./sales-wizard";
export const dynamic="force-dynamic";
export default async function NewSale({searchParams}:{searchParams:Promise<{customer?:string;bike?:string}>}){const defaults=await searchParams;const db=getSupabaseAdmin();const [customers,stock]=await Promise.all([db.from("crm_customers").select("id,first_name,last_name,email,phone,house_name_number,street,address_line_1,address_line_2,city,county,postcode,country,latitude,longitude").is("archived_at",null).order("last_name"),db.from("stock_bikes").select("id,registration,vin,stock_number,make,model,variant,year,mileage,price,status,primary_image_url").in("status",["In Stock","ON FORECOURT","Available","Prep"]).order("created_at",{ascending:false})]);return <AdminPage title="Complete a motorcycle sale" sub="A guided workflow from customer and lead through reservation, payment and delivery."><SalesWizard customers={(customers.data??[]) as never[]} stock={(stock.data??[]) as never[]} defaultCustomer={defaults.customer??""} defaultBike={defaults.bike??""}/></AdminPage>}
