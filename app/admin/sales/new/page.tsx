import {AdminPage} from "../../dashboard/page";import {getSupabaseAdmin} from "@/lib/supabase/admin";import {SalesWizard} from "./sales-wizard";
export const dynamic="force-dynamic";
export default async function NewSale({searchParams}:{searchParams:Promise<{customer?:string;bike?:string}>}){const defaults=await searchParams;const db=getSupabaseAdmin();const [customers,selectedCustomer,stock]=await Promise.all([db.from("crm_customers").select("id,title,first_name,last_name,email,phone,alternate_phone,house_name_number,street,address_line_1,address_line_2,address_line_3,city,county,postcode,country,latitude,longitude,customer_status,tags,notes").is("archived_at",null).neq("is_test_record",true).order("updated_at",{ascending:false}).limit(50),defaults.customer?db.from("crm_customers").select("id,title,first_name,last_name,email,phone,alternate_phone,house_name_number,street,address_line_1,address_line_2,address_line_3,city,county,postcode,country,latitude,longitude,customer_status,tags,notes").eq("id",defaults.customer).maybeSingle():Promise.resolve({data:null}),db.from("stock_bikes").select("id,registration,vin,stock_number,make,model,variant,year,mileage,price,status,primary_image_url").neq("is_test_record",true).in("status",["In Stock","ON FORECOURT","Available","Prep"]).order("created_at",{ascending:false})]);const customerRows=[...(customers.data??[])];const selected=selectedCustomer.data;if(selected&&!customerRows.some(item=>item.id===selected.id))customerRows.unshift(selected);return <AdminPage title="Complete a motorcycle sale" sub="A guided workflow from customer and lead through reservation, payment and delivery."><SalesWizard customers={customerRows as never[]} stock={(stock.data??[]) as never[]} defaultCustomer={defaults.customer??""} defaultBike={defaults.bike??""}/></AdminPage>}
