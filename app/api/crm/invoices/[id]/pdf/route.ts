import {NextResponse} from "next/server";import {getInvoiceDocument} from "@/lib/invoice-document";import {buildInvoicePdf} from "@/lib/invoice-pdf";export const runtime="nodejs";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){try{const d=await getInvoiceDocument((await params).id);const bytes=await buildInvoicePdf(d);return new NextResponse(Buffer.from(bytes),{headers:{"Content-Type":"application/pdf","Content-Disposition":`attachment; filename="${d.invoice.invoice_number}.pdf"`,"Cache-Control":"no-store"}})}catch(error){console.error("Invoice PDF failed",error instanceof Error?error.message:error);return NextResponse.json({error:"Unable to generate invoice PDF."},{status:500})}}
