import {NextResponse} from "next/server";import {getSupabaseAdmin} from "@/lib/supabase/admin";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{const invoice_id=(await params).id;const b=await request.json() as {description?:string;quantity?:number;unit_price?:number;item_type?:string};if(!b.description?.trim())return NextResponse.json({error:"Description is required."},{status:400});const {data,error}=await getSupabaseAdmin().from("crm_invoice_items").insert({invoice_id,description:b.description.trim(),quantity:Number(b.quantity)||1,unit_price:Number(b.unit_price)||0,item_type:b.item_type||"other",sort_order:100}).select().single();if(error)throw error;return NextResponse.json({item:data},{status:201})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Unable to add item."},{status:500})}}
export async function DELETE(request:Request,{params}:{params:Promise<{id:string}>}){try{const invoice_id=(await params).id;const itemId=new URL(request.url).searchParams.get("item");if(!itemId)return NextResponse.json({error:"Item is required."},{status:400});const {error}=await getSupabaseAdmin().from("crm_invoice_items").delete().eq("id",itemId).eq("invoice_id",invoice_id);if(error)throw error;return NextResponse.json({ok:true})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Unable to remove item."},{status:500})}}
